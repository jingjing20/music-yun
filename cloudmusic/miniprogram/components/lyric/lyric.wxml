<scroll-view hidden="{{isLyricShow}}" class="lyric-scroll" scroll-y="{{true}}" scroll-with-animation="ture" scroll-top="{{scrollTop}}">
  <view class="lyric-panel">
    <block wx:for="{{lrcList}}" wx:key="item">
      <view class="lyric {{index==nowLyricIndex?'height-ligth':''}}">{{item.lrc}}</view>
    </block>
  </view>
</scroll-view>